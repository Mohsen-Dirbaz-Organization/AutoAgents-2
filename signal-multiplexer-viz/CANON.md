# CANON — generated from `src/data/canon/`

> **Do not edit this file.** It is a rendering of the canonical registers
> (`node scripts/render-canon.mjs`). Edit the registers; regenerate.
> Validator: **RELEASABLE** — 0 blocking, 2 listed.

## Definition of Done
- ✅ **DoD-1** — Claims carry source/standing/scope/owner/falsifier/artifact
- ✅ **DoD-2** — Gates carry typed interface/invariant/provenanced threshold/failure/reason/test vector
- ✅ **DoD-3** — State transitions reconstructable from ordered events (Archive + snapshots; engines expose getState snapshots) *(structural — observed in the Constitution view (versioned Archive, rollback))*
- ✅ **DoD-4** — Numbers labelled + configured + recompute in the validator
- ✅ **DoD-5** — Symbols resolve to one type each
- ✅ **DoD-6** — Hazard↔requirement orphans listed in both directions
- ✅ **DoD-7** — External docs rendered from the ledger (registers are the source of truth) *(structural — README obligation tables now defer to canon/obligations.js)*
- ✅ **DoD-8** — No guaranteed-pass checks; retirements preserved

## Claim-standing ladder
| Standing | Meaning | Authority |
|---|---|---|
| **Established** | Proof or reproducible measurement with stated scope | May support the safety case |
| **Constructed** | True by explicit definition or architecture; supports design semantics only | Design semantics only — never empirical evidence |
| **Proposed** | Coherent design with named proof or test debt | Requires a validation pack |
| **Target** | Budget, threshold or roadmap objective; never phrased as achieved | Cannot expand authority; cannot be phrased as achieved |
| **Unsupported** | No artifact in the delivered corpus | Quarantine |

## Claims (8)
| Claim | Standing | Scope | Falsifier | Artifact |
|---|---|---|---|---|
| As risk rises the admissible action set only contracts: R1 ≤ R2 ⇒ U_ad(R2) ⊆ U_ad(R1). | constructed | Enforced by construction in the simulation (prefix-complement admissible set). In-sim violation rate is a separate simulated number (num.monotonicity_violation_rate). Real-device behaviour is an open obligation. | Any tick where risk rose and admittedCount grew with conservative discipline ON. | `src/simulation/BoundedAutonomyStack.js#_computeAdmissibleSet/_checkMonotonicity` |
| The CRL keeps the weighted log-gain budget at Q = 0 while redistributing loudness; the conserved coordinate reads only the gauge-fixed shape. | constructed | Q = 0 after projection and shape-invariance are definitional identities of the implemented operator — design semantics, not evidence. | A rounding fault: \|Q\| or drift exceeding tol_conserve (diagnostic, not verification). | `src/simulation/ConservationRenormalizationLayer.js#step` |
| §3.4(ii): a compensating gain cannot mask a genuine graded defect from the gauge-fixed shape band. | established | IN-SIM: adversarial masking probe passes live each tick and fails when sabotaged (raw band). Not field/hardware evidence. | maskingProbe() returning masked=true on the honest shape band; or noOpAudit() classifying the probe as guaranteed-pass. | `src/simulation/ConservationRenormalizationLayer.js#maskingProbe/noOpAudit` |
| xi_feature correlates monotonically with physical risk in automotive perception. | proposed | Validated at bin level on a gasification corpus ONLY. In this sim xi_feature is driven from risk.R — a modelling choice that must not be cited as evidence. phi_risk requires a one-sided calibration that does not exist. | Cross-domain study with correlation r ≤ 0.7 on an automotive corpus. | `src/simulation/BoundedAutonomyStack.js#_runCompiler (driven, not evidential)` |
| The governance layer keeps the system correctable: challenges demote canon, silent drift is forced to surface, rollback restores prior epistemic state, erasure leaves lineage. | established | IN-SIM: demonstrated in a real browser (challenge → Warranted→Provisional; drift surfaced; rollback restores canon). Governs the simulation, not a vehicle. | A truth-status change that produces no constitutional event in the Archive; or a rollback that fails to restore the snapshot canon. | `src/simulation/ConstitutionalTruthEngine.js` |
| The ~32 ns analog-veto witness is the only measured latency in the corpus; every other timing figure is a target. | established | Cited upstream measurement (cfg-fpga-veto-upstream). This repo performs no timing measurement. | A second measured latency with a configuration artifact; or retraction of the upstream measurement. | `canon/numbers.js#num.veto_latency_ns` |
| The "144 unique latencies" claim is not confirmed under any tested counting convention (248 records / 237 exact / 240 & 229 unique raw / 84 endpoints / 68 exact values). | established | Holds for the parse conventions tested; a canonical upstream convention could still reconcile it (open). | An upstream counting convention that reproduces 144 from the raw records. | `F26_Autonomous_Driving_Event_Fabric_Plan.md#latency-and-race-investigation` |
| Program coverage (15/20/10/11 over 56) measures source-material availability in the design corpus — never fabricated or measured silicon. | constructed | A property of the mapping's definition of "coverage". | n/a for the definition; the counts themselves are derived numbers recomputed by the validator. | `src/data/programCoverage.js` |

## Number cards (17)
| ID | Quantity | Value | Kind | Config | Uncertainty |
|---|---|---|---|---|---|
| `num.veto_latency_ns` | Analog-veto witness latency | 32 ns | measured | `cfg-fpga-veto-upstream` | approximate ("~32 ns"); upstream does not state an interval |
| `num.epu_gate_ns` | EPU accept/reject gate budget | 3 ns | target | `cfg-none` | n/a (target) |
| `num.witness_bitvector_ns` | Witness bitvector evaluation budget | 8 ns | target | `cfg-none` | n/a (target) |
| `num.gap_effort_pd_lo` | Coverage-gap closure effort (lower bound) | 55 person-days | target | `cfg-none` | range with num.gap_effort_pd_hi |
| `num.gap_effort_pd_hi` | Coverage-gap closure effort (upper bound) | 80 person-days | target | `cfg-none` | range with num.gap_effort_pd_lo |
| `num.tau_boundary_s` | Analog/digital residence boundary τ | 5 s | constructed | `cfg-sim-harness` | exact by definition |
| `num.tick_ms` | Simulation tick interval | 100 ms | constructed | `cfg-sim-harness` | browser timer jitter, unquantified |
| `num.monotonicity_violation_rate` | Antitone monotonicity violation rate under conservative discipline | 0 % | simulated | `cfg-sim-harness` | exact for observed runs; not a proof over all runs |
| `num.latency_records_total` | F26 bundle latency-like records (parse) | 248 records | derived ↻ | `cfg-sim-harness` | exact for the parse convention used |
| `num.latency_records_exact` | Exact "Latency:" records in the parse | 237 records | derived | `cfg-sim-harness` | exact for the convention |
| `num.latency_records_nonexact` | Variable/approximate/range/bound latency records | 11 records | derived | `cfg-sim-harness` | exact for the convention |
| `num.gap_count` | GAP-coverage subcategories | 11 subcategories | derived ↻ | `cfg-sim-harness` | exact |
| `num.coverage_full` | FULL-coverage subcategories | 15 subcategories | derived | `cfg-sim-harness` | exact |
| `num.coverage_high` | HIGH-coverage subcategories | 20 subcategories | derived | `cfg-sim-harness` | exact |
| `num.coverage_partial` | PARTIAL-coverage subcategories | 10 subcategories | derived | `cfg-sim-harness` | exact |
| `num.coverage_total` | Total research subcategories | 56 subcategories | derived | `cfg-sim-harness` | exact |
| `num.claim_144_unique_latencies` | "144 unique latencies" (upstream claim) | 144 latencies | target | `cfg-none` | unconfirmed |

↻ = recomputed from stated inputs by the validator on every run.

## Symbol collision register (6)
| Glyph | Conflict | Canonical names | In this repo |
|---|---|---|---|
| σᵢⱼ | Rank-2 field vs scalar score vs scalar function vs admissible set — four types | `J_active (active-constraint Jacobian)`, `h_score`, `V_lyap`, `X_state / U_input` | Not used — the stress-tensor reading (T5) is retired; see retirements.js |
| S | Integer parity vs safety construct vs matrix vs state function | `S_parity`, `S_safety`, `S_between / S_within`, `s_entropy` | BoundedAutonomyStack.scalars.S = S_parity (±1 structural parity). S_safety is NOT computed anywhere in this repo. |
| ξ | Unitless feature vs asserted one-sided risk quantity | `xi_feature`, `phi_risk (only after one-sided calibration — which does not exist)` | BoundedAutonomyStack.scalars.xi = xi_feature. In the sim it is DRIVEN from risk.R (a modelling choice); the claim "ξ tracks physical risk" is proposed, not established. |
| ε | Numeric tolerance vs control parameter vs privacy parameter vs undeclared residual bound | `tol_closure`, `eps_action`, `eps_dp`, `tol_conserve (MUST be derived from the arithmetic format)` | eps_correlation (memory/claim aging, 0.05, §9.1.1) in BoundedAutonomyStack._ageMemory + ConstitutionalTruthEngine._ageClaims; tol_conserve derived in ConservationRenormalizationLayer.tolConserve() from Number.EPSILON. |
| ρ | Unitless feature vs physical density vs operator norm | `rho_rigidity`, `rho_mass`, `spec_radius` | Not used as a state symbol in this repo. |
| φ | Mathematical constant vs calibrated risk output vs field | `phi_golden (constant, never a variable)`, `phi_risk`, `Phi_potential` | The "φ-compiler" naming refers to the conservation-manifold compiler producing xi_feature/S_parity; no phi_risk exists (no one-sided calibration). |

## Gate contracts (7)
| Gate | Invariant | Threshold | Provenance | Reason code |
|---|---|---|---|---|
| Antitone monotonicity check | R > prevR ⇒ admitted ≤ prevAdmitted | admitted > prevAdmitted while risk rose | hazard hz.unauthorized_expansion — an expanding envelope under rising risk is the top-level hazard the whole stack exists to prevent | `MONO-EXPAND` |
| Analog veto (S4) | A sharp risk spike arms the electrically-isolated guard before any classifier resolves the scene. | dR > 0.18 or R > 0.85 | hazard hz.late_refusal — the veto must precede classification; the ~32 ns witness (num.veto_latency_ns) is the cited timing basis | `VETO-SPIKE` |
| Conservation residual check (CRL) | \|Q\| after projection ≤ tol_conserve (derived from the arithmetic format) | tolConserve(M) ≈ 6e-14 for M=5 | derived from IEEE-754 binary64 EPSILON per companion P1 — NOT a chosen constant | `CONS-RESIDUAL` |
| Adversarial masking probe (§3.4(ii), live) | A genuine graded defect remains detected in the gauge-fixed shape band under an adversarial compensating gain. | ε = 0.05 defect band | hazard hz.masked_defect — a masked sensor defect admits unwarranted fusion authority | `MASK-EVADE` |
| LLC drift quarantine | A consolidation coinciding with an LLC jump is not committed. | Δllc > 0.25 per tick | hazard hz.silent_drift — developmental phase transitions must not silently enter strategic memory | `LLC-JUMP` |
| τ-boundary re-quantization (Lane C) | No analog state crosses τ = 5 s; crossing requires cross-modality confirmation and safe-side rounding. | residence ≥ 5 s AND ΔJ/C ≥ 1.0 | ratified Numerical Substrate Partition ADR (num.tau_boundary_s) + hazard hz.analog_decay — decayed analog state must not masquerade as durable truth | `TAU-CROSS` |
| Constitutional challenge adjudication | Correction supremacy: counter-evidence stronger than challengeResistance demotes; weaker strengthens resistance. Either way a constitutional event is archived (anti-silent-drift). | strength > warrant.challengeResistance | hazard hz.silent_drift + Art. XVIII/XIX of the Constitution source | `CHAL-ADJ` |

## Retirement register (4) — removed, not owed
- **Constraint stress tensor / "constraint satisfaction as stress fields" (Source #3 Part V, derivative T5)** — Undefined: the Cauchy stress tensor is DERIVED from mass density, traction, momentum and subbody balance — none of which exist on a state manifold. What the idea actually uses is the active-constraint Jacobian, i.e. a polytope, which MPC already provides. The trace needs a metric never given; the determinant is not a polytope volume; the "yield surface" is generically empty. *Disposition:* REMOVED from the backlog (was "not yet built" in the README). Do not rebuild a stress-field panel.
- **Landauer-cost accounting for memory/erasure decisions** — Inert: the irreversibility premise is not expressible in the deck's own formalism (translation operators are invertible; irreversibility lives in state-map non-injectivity, never written down); a dissipative ODE produces no Landauer cost without a physical embedding; and kT·ln2 ≈ 2.87e-21 J/bit is 6–9 orders below CMOS switching — every derived recommendation optimizes a negligible term. *Disposition:* RETIRED-INBOUND: never adopted in this repo; recorded so it cannot enter via a future source. Memory erasure here is justified by correlation decay + τ-hierarchy (Part II), not thermodynamics.
- **"Bianchi identity" constraint-consistency check** — A guaranteed-pass no-op occupying the terminal governance position: the (misnamed) identity is automatically true for any affine connection, so it has no truth value to lose. Consistency is satisfiability (feasible set non-empty) — a global combinatorial property, not a local differential one. *Disposition:* RETIRED-INBOUND, and generalized: the noOpAudit() tautology detector screens EVERY registered check for the guaranteed-pass pattern.
- **§3.4 clauses (i)/(iii) asserted as live per-tick VERIFICATION** — conservedDrift compares unit(shape·g) to shape — identical for any g>0; residual is \|Q\| after a projection that removes the weighted mean by algebraic identity. Neither can fail; both were advertised as "re-confirmed live every tick". *Disposition:* REMOVED as verification; retained as rounding-fault DIAGNOSTICS with standing constructed. The live check is now the adversarial masking probe, which can fail.

## Open obligations (P0–P3)
| Pri | Obligation | Owner | Status | Absorbs |
|---|---|---|---|---|
| P0 | Retire the retired vocabulary everywhere; apply the errata at source. | this repo (documentation & release) | closed | — |
| P0 | Withdraw "achieved" phrasing from any claim lacking an artifact identifier; every number carries kind + configuration. | this repo | closed | — |
| P1 | Threshold provenance for every gate, derived from hazards. | this repo (sim gates); systems safety (vehicle gates) | closed-in-sim | — |
| P1 | Conservation tolerance per check, derived from the arithmetic format. | this repo (numerical methods) | closed | — |
| P1 | Monotone admission must survive device non-ideality on MEASURED memristor models (violation rate → 0 under discipline). | PoC Workstream A (external) | open | O-10, Void V-1 (device slice) |
| P1 | φ-compiler transfer gasification → automotive: cross-domain correlation r > 0.7 + deterministic-surrogate rank corr ≥ 0.85. | Conservation-Manifold Compiler team (external) | open | O-3, O-11 |
| P2 | Formal bounds still owed: one-sided bound for xi_feature (O-1); Lyapunov/convergence for the CQR flow (O-2); rigorous finite-N invariant for S_parity (O-5); impulsive stability (O-7); finite-time ergodic guarantee (O-8). | formal analysis (open) | open | O-1, O-2, O-5, O-7, O-8, Void V-1 (proof slice) |
| P2 | Close the 11 GAP subcategories of the coverage map (num.gap_effort_pd_lo–hi person-days of source acquisition). | program planning | open | Void V-2 |
| P2 | Canonical counting convention + reconciled ledger for the bundle's latency records (the "144" claim stays NOT CONFIRMED until then); itemize the 248 cards. | F26 integration / upstream bundle | open | F26 unresolved claim #1 |
| P3 | Registry hygiene for Source #1–#3 assessments (identity anchors, constraint scoring) — now as register EXTENSIONS, not new documents. | this repo | open | Void V-4 |
| P3 | Review charter with the contradiction register as its first matter. | governance (external) | open | — |
| P2 | WCET of the simplex projection: resolved architecturally — projection off the hard real-time path; only the latched S_parity-bit read is on it. | architecture | closed | O-4 |
| P2 | Discrete channel-space geometry: define the metric or flag as heuristic. | this repo | closed | O-9 |
| P2 | Multiplexer simulation must be view-scoped (was App-scoped, kept ticking in the background). | this repo | closed | Void V-3 |

## Validator findings (2)
- **WARNING** `C6:hz.device_nonideality` — ORPHAN HAZARD (no mechanism): Real memristor non-ideality (drift, read noise, retention loss) re-admits forbidden commands on physical hardware.
- **WARNING** `C6:req.field_validation` — ORPHAN REQUIREMENT (no hazard traces to it): CRL/TPD/MMR require field validation before any trusted promotion (source §8).
