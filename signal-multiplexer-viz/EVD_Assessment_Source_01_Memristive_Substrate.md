# EVD Assessment — Source #1: *Bounded Autonomy on a Memristive Substrate*

> **Protocol:** Extraction of Structured Derivatives from Intertwined Texts — **v0.2**
> **Execution date:** 2026-06-11 · **Assessor:** Claude (automated extraction pipeline)
> **Reference layer:** `signal-multiplexer-viz/README.md` design space (the CQR / topological-`S` /
> Dirac-impulse / three-scale-RG-ladder / EPU-hardware spine)
> **Companion record:** integrated summary lives in the README's *Design-Space Integration Registry*;
> this file is the **complete §8 output-format** instance the registry summarizes.

This document is the full protocol output for Source #1. It is produced in the order the protocol
specifies: invocation record (§12) → output header (§8.2) → reference layer (§8.3) → entanglement map
(§8.4) → dependency diagram (§8.5) → angular positioning table (§8.6) → synergy matrix (§8.7) →
common ground (§8.8) → per-derivative writing templates (§8.9) → verification summary (§8.10) →
validation checklist (§11) → net-position narrative.

---

## 0. Status Legend (Production Standard §10.1.2)

The protocol's status color coding is reproduced here in text form; every status token below maps to
the protocol's palette.

| Token | Meaning | Protocol color (RGB / Hex) |
|-------|---------|----------------------------|
| 🟢 **PASS / CLEAR / ADMITTED** | Check passes; content admitted | Forest Green (34,139,34) `#228B22` |
| 🟡 **BORDERLINE / UNCLEAR** | Acceptable with documented caveat | Goldenrod (218,165,32) `#DAA520` |
| 🔴 **FAIL / REJECTED** | Check fails; content rejected | Crimson (220,20,60) `#DC143C` |
| ⚪ **PENDING / NOT-EVALUATED** | Deferred / out of scope here | Gray (128,128,128) `#808080` |

---

## 1. Invocation Record (§12)

```
INVOKE PROTOCOL "Extraction of Structured Derivatives from Intertwined Texts" v0.2
WITH REFERENCE LAYER = signal-multiplexer-viz/README.md design spine (Format 3: Conceptual Anchor)

MODIFIERS (enabled for this run):
[x] Pre-specify conceptual units: concepts ............. 8 derivatives + 1 common ground
[x] Include visual/tabular map of concept intersections: map entanglement ....... §4
[x] Include functional dependency graph: dependency graph ........................ §5
[x] Include 3D angular coordinate table: angular positioning chart ............... §6
[x] Include reference-conditional pairwise synergy analysis: matrix .............. §7
[x] Extract common content separately: common ground ............................ §8
[x] Include connection verification table: verify bindings ...................... §10
[x] Include functional independence scores: assess independence ................. §10
[x] Include mean pairwise distance and diversity metrics: angular diversity ..... §10
[x] Limit number of derivatives (merge smaller concepts): N max = 10 ............ 8 ≤ 10 ✓
[x] Guidance on acceptable output entanglement density: target density = Low ..... §10
[x] Minimum acceptable functional independence score: min = 60% ................. §10
[x] Minimum acceptable mean pairwise distance: min = 0.30 ....................... 0.42 ✓
[ ] Include complete evidence package per §3.5 ................................. summarized
[ ] Include JSON output per §4.5: generate visualization export ................ deferred (⚪)
```

**Note on nested extraction (§13).** The source is **5,910 words** (> 5,000), which nominally triggers
the nested-extraction recommendation. It is handled here as a **single-level, 8-derivative** extraction
because the source is *already* cleanly pre-segmented into eight architectural lanes (§2, Lanes A–H), so
the natural conceptual units are visible at the top level; a second pass would over-segment (§9.4). The
lane structure is treated as the author's own first-pass clustering.

---

## 2. Output Header (§8.2)

| Field | Value |
|-------|-------|
| **Extraction Title** | Bounded Autonomy on a Memristive Substrate — Engineering Technical Reference |
| **Source** | `baf147c7-Bounded_Autonomy_Memristive_Substrate_Technical_Reference.md` (Rev 2026-05-26) |
| **Word Count** | ≈ 5,910 words |
| **Entanglement Density** | 🟡 **High** — the monotonicity invariant threads through all eight lanes; concepts are fused at clause level, so decomposition + cross-referencing is mandatory (not separable by section alone) |
| **Identified Conceptual Units** | **8** derivatives + **1** common ground (≤ 10 → no over-segmentation, §9.4) |
| **Concept List** | D1 Antitone Monotonicity Invariant · D2 Enforced Causality as Fidelity · D3 Metabolic-Memory Stratification · D4 Budgeted Retrieval / Shadow Price λ · D5 Conservation-Manifold Compiler φ→ξ/S · D6 Architecture of Refusal / Analog Veto (S0–S4) · D7 Memristor Co-Design & τ=5 s Boundary · D8 Anti-Silent-Drift / LLC Governance |

**Document Identification Table (§10.2.3)**

| Field | Value |
|-------|-------|
| Filename | `baf147c7-Bounded_Autonomy_Memristive_Substrate_Technical_Reference.md` |
| Proposed Title | *Bounded Autonomy on a Memristive Substrate (Technical Reference)* |
| Classification | Engineering technical reference / architecture synthesis (projected-vs-measured ledgered) |
| Primary Concept | The **antitone monotonicity invariant** — error may shrink, never enlarge, the admissible set |
| Entanglement Density | 🟡 High |

**Gate check (§5.3 Automatic Rejection):** 🟢 **PASS** — source is identifiable, carries its own
provenance (Rev 2026-05-26), maintains a known/built/claimed/unverified evidentiary ledger (§7), and
supplies the required evidence components (claims + mechanisms + instances + boundaries). **Not rejected.**

---

## 3. Reference Layer (§8.3 → Format 3: Conceptual Anchor)

The reference layer is **this repository's existing design space**, used as the stable contextual anchor
against which every derivative is positioned. Per §3.2 Format 3 (Conceptual Anchor):

- **Core concept:** A *self-healing metabolic-memory multiplexer* for multi-agent systems, governed by
  conserved-quantity renormalization (CQR), with a three-scale renormalization-group ladder
  (**Gate → Tracking-Buffer → Memory-Bank**, fast→slow), a topological structural invariant `S`,
  Dirac-impulse consensus/coverage operators, and an EPU-style hardware-mapping overlay onto digital ICs.
- **Relevant dimensions (the spine's axes the derivatives attach to):**
  1. **Conserved scalars** — CQR's `ξ = ln(k/Λ_G)` continuous invariant and a discrete structural `S`.
  2. **Scale ladder** — three residence/time scales with verification strengthening down the ladder.
  3. **Contestability** — a warrant / defeater / standing ledger (every claim is challengeable).
  4. **Hardware overlay** — mapping the abstract pipeline onto physical compute (digital-IC EPU Gates).
  5. **Impulse operators** — Dirac-δ corrections for discrete consensus and ergodic coverage.
- **Interpretive lens:** Position a derivative by *how far it diverges from this spine*. Origin
  `(0,0,0)` = "restates the spine in the spine's own vocabulary." Low β = speaks the spine's language;
  high β = reaches in from another domain (device physics / automotive). Low α = concrete instance/hardware;
  high α = abstract law. Low γ = portable method; high γ = deeply domain-embedded.
- **Reference-layer challenges (RC) the positioning must answer:**
  - **RC1** — Where does the new substrate attach to the existing three-scale ladder?
  - **RC2** — What does the source *add* that the spine lacks?
  - **RC3** — How is safety / contestability preserved across the new analog/digital seam?
  - **RC4** — Does the source *confirm, extend, or compete with* the ξ/S/monotonicity spine?

**Reference-layer well-formedness (§11 check):** 🟢 PASS — the anchor is concrete (named scalars, named
ladder, named hardware overlay), and is neither too abstract nor too distant from the source (the source
*shares the same ξ/S engine*), so §9.12 (disconnection from reference layer) does not apply.

---

## 4. Entanglement Map (§8.4)

High-density intersection points where two or more concepts share the same sentence/paragraph/section,
with link type ∈ {causal, definitional, analogical, normative, methodological} and intensity.

| # | Intersection point (source location) | Concepts fused | Link type | Intensity |
|---|--------------------------------------|----------------|-----------|:---------:|
| E1 | §1 ¶3 — the load-bearing sentence ("admissible action set may only ever contract") | D1 ↔ D2 ↔ (all) | **Normative** (the one invariant everything protects) | 🔴 High |
| E2 | §1 ¶2 — "fidelity leans toward manipulated causality rather than accuracy" | D2 ↔ D1 | **Definitional** (fidelity redefined) | 🔴 High |
| E3 | §2 table — "two laws meet at the EPU" (authority top-down, consequence bottom-up) | D6 ↔ D1 ↔ D8 | **Causal** (consequence law = narrowing) | 🟡 Medium |
| E4 | §2.4 / §8 glossary — φ emits ξ and S, consumed by S1 gate | D5 ↔ D6 ↔ D1 | **Methodological + definitional** | 🔴 High |
| E5 | §3 ¶"(1)" + §3 table — stratify by residence time; verify ∝ τ | D3 ↔ D1 ↔ D8 | **Normative** ("verification strengthens monotonically with τ") | 🔴 High |
| E6 | §3 ¶"(2)" — retrieval as budgeted optimization, shadow price λ | D4 ↔ D3 | **Methodological** (λ gates retrieval *within* a tier) | 🟡 Medium |
| E7 | §3 ¶"(3)" — enforced causality = manipulated ingest weight = boundary flux b(y) | D2 ↔ D1 ↔ D3 | **Causal** (ingestion-time projection of downstream flux) | 🔴 High |
| E8 | §3 "Anti-silent-drift coupling" — LLC jump quarantines a strategic consolidation | D8 ↔ D3 ↔ D5 | **Normative + methodological** | 🟡 Medium |
| E9 | §4.1 — three policy commitments map onto device physics (retention/potentiation/Arrhenius) | D7 ↔ D3 ↔ D4 | **Analogical** (policy ↔ device-physics isomorphism) | 🔴 High |
| E10 | §4.2 — τ=5 s boundary: analog barred from strategic; ADR-ratified | D7 ↔ D3 ↔ D8 | **Normative** (boundary placed where blast radius grows) | 🔴 High |
| E11 | §2.2 / §7 — "only the FPGA witness latency (~32 ns) is measured" | D6 ↔ Common Ground | **Definitional** (the projected/measured seam) | 🔴 High |
| E12 | §6 — Posit/quire, symplectic integrators, β-recurrence as substrate requirements | D3 ↔ D7 ↔ D1 | **Methodological** (numerical substrate honoring monotonicity) | 🟡 Medium |

**Entanglement reading:** the **density is High** because E1, E2, E4, E5, E7, E9, E10, E11 each fuse
≥ 2 concepts at the clause level around a *single* invariant (D1). This is why the protocol's
decomposition-with-cross-referencing (not section-splitting) is required — the invariant is the through-line,
so D1 is referenced by nearly every other derivative rather than owning a contiguous block of text.

---

## 5. Dependency Diagram (§8.5)

Functional dependencies as a DAG; `A → B` reads "**A** needs **B** to be fully understood / **A** depends
on **B** for completeness." Root of the DAG is **D1** (the invariant the whole document protects), which
is why it has in-degree from nearly all others — characteristic of a single load-bearing law.

```
                         ┌──────────────────────────── D1  (Antitone Monotonicity Invariant)
                         │            ▲        ▲     ▲
        D2 ──────────────┘            │        │     │   (enforced causality lifts the law into perception)
        D3 ───────────────────────────┘        │     │   (residence-tier verification IS monotone-with-τ)
        D5 ────────────────────────────────────┘     │   (ξ/S are the bounded scalars realizing one-sidedness)
        D6 ──────────────────────────────────────────┘   (the S0–S4 cascade enforces narrowing in hardware)

        D4 ── → D3                 (shadow price λ_k prices retrieval *within* a residence tier)
        D7 ── → D3                 (device retention windows are mapped onto the residence tiers)
        D8 ── → D5                 (the LLC phase label sits beside φ's discrete structural-parity S)

        Common Ground ── → (ALL)   (the projected/measured evidentiary seam governs every claim)
```

**Dependency edge list (for traceability):**

| Edge | Reads as | Source warrant |
|------|----------|----------------|
| D2 → D1 | enforced causality is the antitone law lifted into the fidelity/perception domain | §3 ¶(3): "Antitone admission lifts the actuation algebra into the fidelity domain" |
| D3 → D1 | residence-stratified verification *is* the "monotone with τ" form of the law | §3 ¶(1): "verification … must strengthen monotonically with τ" |
| D4 → D3 | the shadow-price test operates per-partition, inside a residence tier | §3 ¶(2): "For partition k with budget B_k …" |
| D5 → D1 | ξ/S are the bounded, saturating scalars that make one-sidedness computable | §2.4: "bounded, saturating metrics that correlate monotonically with physical risk" |
| D6 → D1 | the S0–S4 cascade is the law re-expressed as "each stage can only narrow the next" | §2.2: "each stage can only narrow what the next is permitted to do" |
| D7 → D3 | device half-life is mapped onto reflexive/tactical/strategic residence | §4.1 / §4.2 tier table |
| D8 → D5 | the LLC discrete phase label is the governance twin of φ's discrete S | §3 anti-drift + §2.4 (S is structural-parity; LLC marks a phase transition) |
| CG → ALL | the projected/measured seam conditions the standing of every other claim | §0/§7: "the seam … is load-bearing and is preserved throughout" |

**Dependency depth = 2** (longest path: `D4 → D3 → D1`). Low depth ⇒ the extraction is shallow and
navigable, not a dependency chain (§14.1 "Dependency Depth: lower is better"). 🟢 PASS.

---

## 6. Angular Positioning Table (§8.6)

Origin `(0,0,0)` = the README spine restated in its own vocabulary. Axes per §4.1–4.3:
**α abstraction** (0 concrete ↔ 1 abstract), **β analogical distance** (0 direct ↔ 1 analogical),
**γ domain-specificity** (0 universal ↔ 1 domain-specific). Coordinates are evidence-based (§6.13);
the per-axis rationale is in each derivative's writing template (§9).

| Derivative | Abstraction (α) | Analogical Distance (β) | Domain-Specificity (γ) | One-line standing vs. spine |
|------------|:---------------:|:-----------------------:|:----------------------:|-----------------------------|
| **D1** Antitone Monotonicity Invariant (§1) | 0.85 | 0.10 | 0.35 | **Confirms + generalizes** the memory-monotonicity invariant |
| **D2** Enforced Causality as Fidelity (§1, §2.3, §3.3) | 0.55 | 0.20 | 0.65 | **Extends** "manipulated causality" into perception/ingestion |
| **D3** Metabolic-Memory Stratification (§2.3, §3) | 0.45 | 0.25 | 0.55 | **Near-isomorphic** to the Gate/TB/MB ladder |
| **D4** Budgeted Retrieval / Shadow Price λ (§3.2) | 0.60 | 0.45 | 0.45 | **New** recall-side economics (orthogonal to CQR) |
| **D5** Conservation-Manifold Compiler φ→ξ/S (§2.4) | 0.70 | 0.05 | 0.40 | **Identity** — same ξ=ln(k/Λ_G) & S engine (anchor) |
| **D6** Architecture of Refusal / Analog Veto S0–S4 (§2.2) | 0.30 | 0.15 | 0.70 | **Anchors** EPU Gate budgets with the only *measured* 32 ns |
| **D7** Memristor Co-Design & τ=5 s Boundary (§4) | 0.20 | 0.55 | 0.90 | **New substrate floor** below the digital-IC EPU |
| **D8** Anti-Silent-Drift / LLC Governance (§2.1, §3) | 0.65 | 0.35 | 0.55 | **New cross-validator** for topological `S` |

**Diversity test (§6.13):** mean pairwise Euclidean distance in (α,β,γ) = **0.4219** (computed over all
28 pairs) → **> 0.30 target → 🟢 well-layered.** Range: min 0.150 (D2–D3), max 0.963 (D1–D7). The three
sub-0.2 pairs — D1–D5 (0.166), D2–D3 (0.150), D4–D8 (0.150) — are *not* an over-segmentation symptom; they
are precisely the **identity/near-isomorphism pairs** (law↔instrument, two Lane-C facets, two governance
modules) and are exactly where the strongest synergies sit (§7). Not all derivatives cluster within a
0.2 sphere ⇒ §9.10 (angular clustering) does not fire.

**Layering pattern (§4.3):** a **diagonal gradient + full spread** — D1/D5 high-α/low-β (abstract laws
near the spine), through D2/D4/D8 mid-α (extensions), down to D6/D7 low-α/high-γ (concrete, domain-embedded
hardware floor). The source spans abstraction *and* domain-specificity rather than clustering.

---

## 7. Synergistic Problem-Solving Matrix (§8.7)

Reference-conditional synergies (§6.14): each is **explicit/strongly-implicit in source**, **actionable**,
**non-trivial** (beyond concatenation), **traceable**, **conditioned on a reference challenge**, and
**angularly explained**. Each entry names *which RC no single derivative solves alone*.

|        | D1 | D3 | D4 | D5 | D6 | D7 | D8 |
|--------|----|----|----|----|----|----|----|
| **D1** | — | — | — | ▲ **RC4** | — | — | ▲ *triplet* |
| **D2** | ▲ **RC2/RC4** | — | — | — | — | — | — |
| **D3** | — | — | ▲ **RC2** | — | — | ▲ **RC1+RC3** | — |
| **D5** | ▲ **RC4** | — | — | — | — | — | ▲ **O-5** |
| **D6** | — | — | — | — | — | ▲ **RC1** | — |

▲ = documented synergy. Expanded, with angular mechanism:

| Pair / set | Angular pattern (why complementary) | Synergy mechanism (non-trivial combination) | Reference challenge solved |
|------------|--------------------------------------|---------------------------------------------|----------------------------|
| **D3 × D7** | β/γ transfer (mid→high domain; β 0.25→0.55, γ 0.55→0.90) | Residence tiers ↔ device-retention classes; τ=5 s boundary places the analog/digital seam *exactly where blast-radius verification demands* — neither concept locates the seam alone | **RC1 + RC3** — substrate attaches *and* stays safe |
| **D1 × D5** | abstraction spread, low β (α 0.85↔0.70) | The antitone **law** (D1) becomes *computable* only when grounded by the bounded saturating **instrument** ξ/S (D5); law without instrument is unfalsifiable, instrument without law is just a scalar | **RC4** — monotonicity becomes computable, not merely asserted |
| **D5 × D8** | adjacent phase-label pair (Δ ≈ 0.34) | φ's discrete structural-parity **S** + the LLC developmental phase-transition → **two independent discrete phase detectors** that can lock/flip against each other | **O-5** — a *second* detector to cross-validate the spine's topological `S` |
| **D6 × D7** | concrete pair, low α (0.30↔0.20) | The **measured** 32 ns analog veto (D6) anchors the memristor WS-5 latency-certification window (D7); the projected device budget gets one empirical reference point | **RC1** — proves the analog floor *can* honor the veto contract |
| **D4 × D3** | abstraction/domain offset (Δ ≈ 0.27) | Per-partition shadow price λ_k gates *retrieval* within each residence tier; adds the recall economics the CQR spine lacks (CQR only *redistributes* conserved budget, it does not *price* recall) | **RC2** — adds recall-side economics the spine lacks |
| **D2 × D1** | direct, abstraction spread (Δ ≈ 0.44) | Enforced causality (D2) carries the antitone law from *actuation* to *perception/ingestion*, widening the invariant's reach to the input boundary | **RC2 / RC4** — widens the invariant's reach |
| **D1 × D5 × D8** *(triplet)* | triangulation across α with two discrete phase labels | A **law** (D1) + a **continuous scalar** ξ (D5) + **two discrete phase labels** S/LLC (D5,D8) → robust multi-path validation: a drift that fools one detector must also fool the law and the other detector | Robust multi-path validation for the **contestability ledger** |

**Synergy coverage:** 6 pairs + 1 triplet over 8 derivatives. Every documented synergy names a reference
challenge it discharges (§9.13 unconditioned-synergies check does **not** fire). All synergies are traceable
to source content (§9.8 false-synergies check does **not** fire).

---

## 8. Common Ground (§8.8)

**Content (applies to the entire text, owned by no single concept):**
**The evidentiary discipline — the projected / measured seam.** The source declares up front (§0) and
enforces throughout (§7) that *every quantitative figure is **projected** unless explicitly marked
**measured***, and that "a ratified *decision* and a published *plan* are not empirical results." Only the
**FPGA analog-veto witness latency (~32 ns)** is measured; every other latency, FIT rate, retention,
monotonicity, and cost figure is a Phase-0 hypothesis. This discipline frames all eight derivatives and is
the direct counterpart of the README's **Contestability Ledger** (warrant / defeater / standing).

**Management option chosen (§7 of protocol):** *Extract separately* — a single Common Ground section that
precedes the derivatives (chosen for clarity; the seam is cross-referenced from D6 and from every "Boundaries"
field rather than repeated in full).

**Angular position:** `(0.50, 0.10, 0.10)` — near the reference origin (low β, low γ: it is universal,
direct framing), with α ≈ 0.50 because "discipline about evidence standing" is a meta-rule, neither a
concrete instance nor a fully abstract law. Distance from each derivative ≥ 0.36, so it does not collide
with any derivative's position.

---

## 9. Writing Template for Each Derivative (§8.9)

Each derivative carries all required components: **Angular Position + Rationale + Claims + Mechanism +
Instances + Boundaries + Functional Dependencies + Relevance to Reference Layer + Synergy Potentials +
Cross-links** (§6.15: every retained item is relevant to the reference layer; §9.1: no type-based
derivatives — these are *concepts*, not "Claims/Examples/Caveats" buckets).

---

### D1 — Antitone Monotonicity Invariant

**Angular Position:** (α, β, γ) = **(0.85, 0.10, 0.35)**

| Axis | Score | Rationale |
|------|:-----:|-----------|
| Abstraction α | 0.85 | It is the document's most abstract object — a one-line algebraic law over an actuation algebra (`R₁ ≤ R₂ ⇒ U_ad(R₂) ⊆ U_ad(R₁)`), re-expressed in every layer's vocabulary. |
| Analogical β | 0.10 | Speaks the spine's own language directly: it *is* the README's memory-monotonicity invariant generalized; almost no analogical reach needed. |
| Domain γ | 0.35 | Stated generally (any risk-indexed admissible set), only lightly tied to the automotive setting. |

**Claims.**
1. Safety is a property of **constraint propagation**, not representational accuracy.
2. Every distortion the system applies is **one-directional, toward conservatism**.
3. Formally: `R₁ ≤ R₂ ⇒ U_ad(R₂) ⊆ U_ad(R₁)` — higher risk admits a *subset* of lower-risk commands.
4. The same law re-appears per layer: perception shrinks the admissible *action* set, memory shrinks the
   admissible *interpretation* set, actuation shrinks the convex *command box*.
5. **Lose it anywhere and "enforced causality" is indistinguishable from a hallucinating stack.**

**Mechanism.** A monotone (antitone) map from risk to the admissible-command set: as warrant for a benign
interpretation falls, the admissible set may only contract. This converts "manipulated causality" from
recklessness into a safety architecture by guaranteeing the manipulation is *one-sided*.

**Instances.** (a) S3 risk-monotone actuation algebra — convex projection (1-Lipschitz) cannot amplify
oscillation (§2.2, mechanism). (b) Memory: "blast radius of a corrupt datum grows with residence time," so
verification strengthens with τ (§3, instance of the same law). (c) Antitone *admission* in the fidelity
domain (§3 ¶3).

**Boundaries.** The invariant is the program's **central risk** (§5): it is *proposed* at S1/S3/S4
(Contributions I–III), not yet proven end-to-end ("Trusted scalar's one-sidedness, end-to-end: **Owed,
not proven**," §7). It is a law, not a measurement.

**Functional Dependencies.** None upward — D1 is the DAG root. (D2, D3, D5, D6 all depend on D1.)

**Relevance to Reference Layer.** Directly **confirms and generalizes** the README's monotonicity
invariant: the spine asserts verification strengthens down the RG ladder; D1 is that assertion stated as a
single algebra spanning perception→memory→actuation. Answers **RC4** (confirm).

**Synergy Potentials.** With **D5** (RC4: law made computable by ξ/S); with **D2** (RC2/RC4: law extended
to perception); anchors the **D1×D5×D8** triplet (robust multi-path validation).

**Cross-links.** → D2 (lifts it to fidelity), → D3 (its "monotone-with-τ" form), → D5 (its instrument),
→ D6 (its hardware enforcement), → D8 (its drift-governance corollary).

---

### D2 — Enforced Causality as Fidelity

**Angular Position:** (α, β, γ) = **(0.55, 0.20, 0.65)**

| Axis | Score | Rationale |
|------|:-----:|-----------|
| Abstraction α | 0.55 | A redefinition (mid-abstract): "fidelity = reliably reinforce safe state transitions," operative rather than purely formal. |
| Analogical β | 0.20 | Close to the spine, but introduces the *deliberate-discard* twist (sacrifice accuracy to guarantee a causal link) that the spine does not state. |
| Domain γ | 0.65 | Anchored in concrete automotive perception (low-trajectory object → arm braking before classification). |

**Claims.**
1. Fidelity is **redefined**: not "how faithfully representation matches the world," but "how reliably the
   data stream reinforces safe state transitions."
2. The system **deliberately discards accuracy** (texture/size) to **guarantee a causal link**.
3. Enforced causality is *active*: it **manipulates the importance weight** of a datum on ingestion,
   *before* classification.
4. A high-threat ingest weight and a high obstacle (Poisson/Laplace boundary) flux `b(y)` are the **same
   conservatism at two points in the chain**.

**Mechanism.** At ingestion, inflate the threat value of a low-trajectory object so that
`low-trajectory object → arm braking manifold` fires regardless of classification outcome — the
ingestion-time projection of the downstream repulsive boundary flux.

**Instances.** (a) "A low-trajectory object arms the braking manifold *before* any classifier resolves what
it is" (§1). (b) D1 acoustic "low-accuracy, high-causality signals" that arm conservative control early
(§2.7). (c) Monosemantic gating: a "pedestrian" admission must be driven by morphology, not adjacent-crosswalk
texture (§2.5).

**Boundaries.** "Indistinguishable from corrupting your own perception **except for** the monotonicity law"
(§1) — D2 is only safe *because* D1 holds; without D1 it is hallucination. Truthful-interface contract
forbids displayed admissibility from diverging from real admissibility (§2.1).

**Functional Dependencies.** **D2 → D1** (enforced causality is sound only under the antitone law).

**Relevance to Reference Layer.** **Extends** the spine's monotone invariant from actuation to the
*input boundary* (perception/ingestion), a reach the README's ladder did not previously articulate.
Answers **RC2** (adds an ingestion-side mechanism) and **RC4** (extends, not competes).

**Synergy Potentials.** With **D1** (RC2/RC4: widen the invariant to perception).

**Cross-links.** → D1 (its safety warrant), → D3 (ingestion happens in the metabolic-memory data plane),
→ D5 (φ certifies the re-weight is licensed by conservation physics, not data fit).

---

### D3 — Metabolic-Memory Stratification

**Angular Position:** (α, β, γ) = **(0.45, 0.25, 0.55)**

| Axis | Score | Rationale |
|------|:-----:|-----------|
| Abstraction α | 0.45 | A concrete policy with tables (partitions, residence τ, storage, predicates) — more structural than abstract. |
| Analogical β | 0.25 | Near-isomorphic to the spine's RG ladder, but cast in a *biological/metabolic* idiom (immune system, half-lives) that is a modest analogical step. |
| Domain γ | 0.55 | Domain-embedded (LiDAR returns, tracked agents, map priors) but the stratification principle is portable. |

**Claims.**
1. Memory is a **metabolic organ**, not storage: data is admitted/held/evicted by **temporal half-lives**.
2. Stratify by **residence time, not data type** (validity at 100 ms ≠ validity at 5 days).
3. The **fast tier is overwritten instantly; the slow tier can rewrite every future drive** → blast radius
   grows with residence time → verification/provenance/anti-drift must **strengthen monotonically with τ**.
4. One eviction-and-retrieval calculus is shared across scales, but a longer residence is **never a weaker
   safety predicate**.
5. Behaves like an **artificial immune system**: cheap fast filtering, selective mid consolidation, rare
   heavily-gated slow rewriting.

**Mechanism.** Three partitions — **Reflexive (ms, ring buffers, physical-limits check)**, **Tactical
(100 ms–5 s, keyed KV store, trajectory-consensus predicate)**, **Strategic (≥ 5 s, persistent graph,
cross-modality confirmation)** — with verification predicate strength rising down the ladder.

**Instances.** (a) The §3 partition table (residence/storage/predicate/serves). (b) Tactical "trajectory
consensus" via symplectic forward rollouts (§3, §6). (c) Strategic = "the only tier that can rewrite
fundamental weights."

**Boundaries.** All retention numbers are **Phase-0 hypotheses** (§7). The stratification is a *policy*;
its device realization (D7) is unproven; the strategic tier is, by ADR, **digital-only**.

**Functional Dependencies.** **D3 → D1** (the residence-tiered verification *is* the monotone-with-τ law).

**Relevance to Reference Layer.** **Near-isomorphic** to the README's three-scale **Gate → Tracking-Buffer
→ Memory-Bank** RG ladder — reflexive/tactical/strategic maps almost one-to-one onto fast/mid/slow.
This is the single strongest structural attachment point. Answers **RC1** (it *is* the ladder, restated).

**Synergy Potentials.** With **D7** (RC1+RC3: residence ↔ device retention, τ=5 s seam placement); with
**D4** (RC2: λ_k prices retrieval within a tier).

**Cross-links.** → D1 (its governing law), → D4 (its retrieval pricing), → D7 (its device realization),
→ D8 (LLC gates strategic consolidation), → Common Ground (numbers are projected).

---

### D4 — Budgeted Retrieval / Shadow Price λ

**Angular Position:** (α, β, γ) = **(0.60, 0.45, 0.45)**

| Axis | Score | Rationale |
|------|:-----:|-----------|
| Abstraction α | 0.60 | A constrained-optimization formulation (knapsack + Lagrangian) — fairly abstract/mathematical. |
| Analogical β | 0.45 | The most analogically *distant* of the near-spine derivatives: imports an economics/operations-research idiom (shadow price, marginal value per unit cost) the CQR spine does not use. |
| Domain γ | 0.45 | The math is domain-general (any budgeted retrieval), lightly tied to the memory data plane. |

**Claims.**
1. **Retrieval is budgeted optimization:** `max_x Σ ΔJ(dᵢ)xᵢ s.t. Σ C(dᵢ)xᵢ ≤ B_k`.
2. Solved in real time via the budget's **shadow price λ_k** (Lagrange multiplier).
3. Local admission test: **`ΔJ(dᵢ)/C(dᵢ) ≥ λ_k`** — a datum is retrieved iff its value per unit cost
   clears the partition's shadow price; otherwise it ages out.
4. "The database structure **literally dictates what the vehicle can remember to learn from**."

**Mechanism.** Per-partition knapsack relaxed by its Lagrange multiplier λ_k, giving a purely *local*
admission test (no global solve), the recall-side analogue of the compute-side cognition allocator
("spend the expensive resource only when expected regret exceeds cost").

**Instances.** (a) The §3 ¶(2) optimization + the λ_k admission inequality. (b) WS-3: make C(dᵢ) the
**measured switching energy** so λ_k becomes a physical joule budget (§4.3) — the device-priced instance.

**Boundaries.** A *recall-economics* layer, orthogonal to CQR's conserved-budget redistribution; ΔJ and C
must be estimable online. Energy-priced λ_k is a Phase-0 hypothesis until the device model calibrates (§5.3 D).

**Functional Dependencies.** **D4 → D3** (the shadow price operates *within* a residence partition).

**Relevance to Reference Layer.** **Adds** something the spine lacks: the README's CQR *redistributes* a
conserved budget but does not **price retrieval**. D4 supplies the missing recall-side economics. Answers
**RC2** (genuine addition).

**Synergy Potentials.** With **D3** (RC2: λ_k gates retrieval per tier); links to **D7** via WS-3 (energy
as the cost term).

**Cross-links.** → D3 (its container), → D7 (switching energy as cost), → Common Ground (joule figures
projected).

---

### D5 — Conservation-Manifold Compiler φ → ξ / S

**Angular Position:** (α, β, γ) = **(0.70, 0.05, 0.40)**

| Axis | Score | Rationale |
|------|:-----:|-----------|
| Abstraction α | 0.70 | A physics-licensed compiler emitting abstract bounded invariants (ξ, S) — high abstraction. |
| Analogical β | 0.05 | **Lowest β in the set** — this is *literally the README's ξ/S engine*; essentially no analogical distance (identity). |
| Domain γ | 0.40 | Validated on biomass gasification, applied to automotive — cross-domain by construction, hence mid-low γ. |

**Claims.**
1. Manipulated causality is made **trustworthy** by licensing the re-weighting with **conservation physics,
   not data fit**.
2. The compiler relocates compute from PDE/CFD solves to **special-function evaluation** (AGM elliptic
   integrals, ~5 iterations; projected 10⁵–10⁶× speed-up) and a **pole-zero-gain transfer-function**
   enforcing mass/energy conservation via passivity/stability/causality.
3. It emits **two trusted scalars**: continuous **log-deviation invariant ξ** and discrete
   **structural-parity invariant S**, bounded and saturating, correlating monotonically with physical risk.
4. These are the ideal "trusted scalars" the S1 gate consumes as **φ**.

**Mechanism.** Conservation-by-construction transfer-function realization → ξ (continuous) and S (discrete)
→ consumed by the S1 fidelity gate so that *discarded perceptual detail can only ever shrink, never enlarge,
the admissible set*.

**Instances.** (a) Fibonacci-sequence state encoding (length 10–15) → three small neural "computational
laboratories" (Linear / Tanh / Exp-Sigmoid) (§2.4). (b) Bin-level gasification validation — "the cross-domain
evidence that conservation-by-construction generalizes — nothing more is claimed."

**Boundaries.** **Validated only at bin-level on biomass gasification, not yet on automotive** (§2.4, §7).
The `sct` metric was **replaced by a deterministic surrogate**; a cross-machine reproducibility study
(rank corr ≥ 0.85) is **owed**.

**Functional Dependencies.** **D5 → D1** (ξ/S are the bounded scalars that make one-sidedness computable).

**Relevance to Reference Layer.** **Identity anchor** — D5 *is* the README's CQR ξ = ln(k/Λ_G) and discrete
S engine, same gasification bin-level validation. This is the tightest possible attachment: the source and
the spine **share the same instrument**. Answers **RC4** (confirm — by identity).

**Synergy Potentials.** With **D1** (RC4: instrument grounds the law); with **D8** (O-5: S + LLC = two
discrete phase detectors); centre of the **D1×D5×D8** triplet.

**Cross-links.** → D1 (the law it instruments), → D6 (S1 consumes φ), → D8 (LLC twins S),
→ D2 (licenses the re-weight). 

---

### D6 — Architecture of Refusal / Analog Veto (S0–S4)

**Angular Position:** (α, β, γ) = **(0.30, 0.15, 0.70)**

| Axis | Score | Rationale |
|------|:-----:|-----------|
| Abstraction α | 0.30 | Concrete pipeline of named hardware stages (S0–S4 + Gov) with electrical isolation — low abstraction. |
| Analogical β | 0.15 | Maps directly onto the README's EPU hardware overlay; small analogical distance. |
| Domain γ | 0.70 | Highly domain-specific (FPGA witness, analog guard electrically isolated from the digital Queen, ASIL-D lockstep). |

**Claims.**
1. The EPU is a single pipeline where **each stage can only narrow what the next is permitted to do**.
2. S4 is an **independent analog guard, electrically isolated from the digital Queen** — it enforces
   narrowing "with electricity rather than code."
3. The hierarchy is **memoryless** (`Worker → Manager → Queen`, MEET conservative join, rate-clamp last)
   so it stores no optimistic state a hallucination could later exploit.
4. **Only the FPGA witness latency (~32 ns) is measured; every other latency, FIT rate, and cost is
   projected.**

**Mechanism.** A monotone narrowing cascade S0→S4 ending in a sub-nanosecond/analog veto to e-brake; the
analog stage is electrically downstream-isolated so "a hallucinating stack physically cannot reach the
actuator first."

**Instances.** (a) The §2.2 S0–S4 table with standings (S0/S2 Foundation; S1/S3/S4 Proposed). (b) The
measured **~32 ns** FPGA analog-veto witness (§2.2, §7) — the corpus's only measurement. (c) Derived from
the verified Guarded-Swarms analog-veto principle (§7 Established).

**Boundaries.** S1/S3/S4 are **Proposed** contributions; only the 32 ns witness and the Guarded-Swarms
principle are established/measured. Latency margin across temperature/aging/supply is a WS-5 *open* question
(§9.3).

**Functional Dependencies.** **D6 → D1** (the cascade is the antitone law enforced in hardware).

**Relevance to Reference Layer.** **Anchors** the README's EPU hardware overlay with its single empirical
datum: the measured 32 ns gives the spine's *projected* Gate budgets (~3 ns / ~8 ns) a real reference point
(seam preserved — still projected). Answers **RC1** (the hardware attachment) and supplies the only measured
value.

**Synergy Potentials.** With **D7** (RC1: measured 32 ns anchors the memristor WS-5 latency window).

**Cross-links.** → D1 (its law), → D5 (S1 consumes φ), → D7 (analog floor beneath it),
→ Common Ground (it *is* the seam's one measured point).

---

### D7 — Memristor Co-Design & τ = 5 s Boundary

**Angular Position:** (α, β, γ) = **(0.20, 0.55, 0.90)**

| Axis | Score | Rationale |
|------|:-----:|-----------|
| Abstraction α | 0.20 | The most concrete derivative — device physics, V-VTEAM models, crossbars, conductance pulses. |
| Analogical β | 0.55 | **Highest β** — reaches in from a *different domain* (analog device physics) to realize a policy written for digital memory; a genuine analogical mapping. |
| Domain γ | 0.90 | **Highest γ** — deeply embedded in memristor device engineering (volatile/diffusive devices, Arrhenius switching, RRAM compact models). |

**Claims.**
1. The learning policy is **nearly a specification for a memristive substrate**: residence half-lives ↔
   engineered conductance retention; manipulated ingest weight ↔ analog potentiation (in-memory crossbar,
   constant time); shadow-price recall ↔ Arrhenius switching energy.
2. Real memristors are **non-monotone** (stochastic switching, asymmetric potentiation/depression, drift,
   read noise) — and **drift is, definitionally, the silent capability mutation the program forbids**.
3. **Resolution (ADR-ratified):** the **τ = 5 s boundary** — *no analog state crosses τ = 5 s*; strategic
   stays digital Posit/quire; analog admitted only to reflexive + tactical.
4. Five workstreams (WS-1…WS-5) discharge the collision; WS-2 (monotonicity under non-ideality) is
   safety-critical and gates the whole analog path.

**Mechanism.** Place the analog/digital boundary *exactly where the policy places verification burden*:
analog where a corrupt datum ages out before its blast radius grows; digital where it could rewrite future
drives. Any tier promoted across τ = 5 s is re-quantized to Posit/quire and re-admitted under cross-modality
confirmation, so conductance state never enters persistence — only a digitized, re-verified scalar does.

**Instances.** (a) §4.2 tier table (Reflexive volatile / Tactical short-retention crossbar / Strategic
digital Posit-quire). (b) WS-2 conservative read/write discipline: "a threat weight may rise but never
silently fall." (c) PoC Workstream A: V-VTEAM + RRAM compact models, monotonicity-violation-rate headline
metric, target zero under discipline (§5.3).

**Boundaries.** **Every memristor retention/monotonicity/latency number is a Phase-0 hypothesis** (§7, §9).
A device class that cannot guarantee monotonicity is **barred from any actuating tier** (§4.3 WS-2; §9.1
kill criterion). The "new floor" is explicitly **non-strategic** by the ADR.

**Functional Dependencies.** **D7 → D3** (device retention is mapped onto the residence tiers).

**Relevance to Reference Layer.** **Adds a new substrate floor** beneath the README's hardware overlay,
which previously bottomed out at digital silicon. Answers **RC1** (attaches below the ladder) and **RC3**
(the τ=5 s seam is the safety-preserving mechanism). New obligation **O-10** (device-monotonicity) is born
here.

**Synergy Potentials.** With **D3** (RC1+RC3: residence ↔ retention, seam placement); with **D6** (RC1:
measured 32 ns anchors WS-5); links to **D4** (WS-3 energy as cost term).

**Cross-links.** → D3 (its policy), → D6 (the veto it must honor), → D8 (drift = governed silent mutation),
→ D4 (energy-priced recall), → Common Ground (all numbers projected).

---

### D8 — Anti-Silent-Drift / LLC Governance

**Angular Position:** (α, β, γ) = **(0.65, 0.35, 0.55)**

| Axis | Score | Rationale |
|------|:-----:|-----------|
| Abstraction α | 0.65 | A statistical-learning-theory construct (Local Learning Coefficient, developmental phase transitions) — fairly abstract. |
| Analogical β | 0.35 | Moderate analogical reach: imports singular-learning-theory phase transitions to govern memory consolidation. |
| Domain γ | 0.55 | Tied to the strategic-partition attack surface, but the LLC detector is a domain-general method. |

**Claims.**
1. The **strategic partition is the only path to fundamental weights** → it is the attack surface for
   silent capability mutation.
2. The **Local Learning Coefficient (LLC)** is estimated online via **preconditioned SGLD**; a consolidating
   policy passes through **discrete developmental phase transitions** signalled by a simultaneous regret drop
   and an LLC jump.
3. **Governance rule:** a strategic consolidation that induces an LLC jump is **not committed** — it is
   flagged, logged as a challengeable event, and **quarantined between drives** until formally verified.
4. Training and inference substrates are **separated**; the on-board substrate executes only signed,
   verified artifacts (anti-silent-drift discipline).

**Mechanism.** An online developmental-phase-transition detector (LLC jump + regret drop) acting as a
gate on weight consolidation, with quarantine + challengeability as the response — the temporal/learning
form of the antitone law (don't let knowledge silently expand admissibility).

**Instances.** (a) §3 "Anti-silent-drift coupling" paragraph. (b) ADR validation hook (b): an
**LLC-detector-clear on every strategic consolidation** (§4.2). (c) WS-4: expose measured conductance drift
as a first-class logged signal feeding the LLC monitor (§4.3).

**Boundaries.** LLC-as-drift-detector is **Established** (singular learning theory, §7) but its automotive
deployment as a quarantine gate is part of the *proposed* governance overlay; it detects, it does not by
itself prove safety.

**Functional Dependencies.** **D8 → D5** (the LLC discrete phase label is the governance twin of φ's
discrete structural-parity S).

**Relevance to Reference Layer.** **Adds a new cross-validator** for the spine's topological invariant `S`:
the LLC jump is a *second, independent* discrete phase-transition signal that can be regressed against `S`
(advancing the README's open obligation **O-5**, topological mapping). Answers **RC2** (genuine addition).

**Synergy Potentials.** With **D5** (O-5: S + LLC = two discrete phase detectors); part of the **D1×D5×D8**
triplet (robust multi-path validation).

**Cross-links.** → D5 (its discrete-invariant twin), → D3 (it gates strategic consolidation), → D7 (drift
as governed silent mutation), → D1 (the law it serves in the learning-time domain).

---

## 10. Verification Summary (§8.10 + §6.12–6.14)

Per-derivative verification. **Connection** = instances bound to principles/claims to mechanisms (§9.1/9.3);
**Grounding** = traceable to a source location (§9.2); **Binding** = claims+mechanism+instances+boundaries
co-located (§9.3); **Composition** = uniform component set; **Independence** = §6.12 independence score;
**Angular Evidence** = §6.13 coordinates content-justified; **Synergies** = §6.14 explicit & conditioned.

| Derivative | Connection | Grounding | Binding | Composition | Independence | Angular Evidence | Synergies |
|------------|:----------:|:---------:|:-------:|:-----------:|:------------:|:----------------:|:---------:|
| **D1** | 🟢 | 🟢 §1 | 🟢 | 🟢 | 🟢 ~90% (root law) | 🟢 | 🟢 RC4 |
| **D2** | 🟢 | 🟢 §1,§3 | 🟢 | 🟢 | 🟡 ~70% (needs D1) | 🟢 | 🟢 RC2/RC4 |
| **D3** | 🟢 | 🟢 §2.3,§3 | 🟢 | 🟢 | 🟢 ~80% | 🟢 | 🟢 RC1/RC2/RC3 |
| **D4** | 🟢 | 🟢 §3.2 | 🟢 | 🟢 | 🟡 ~75% (needs D3) | 🟢 | 🟢 RC2 |
| **D5** | 🟢 | 🟢 §2.4 | 🟢 | 🟢 | 🟢 ~85% | 🟢 | 🟢 RC4/O-5 |
| **D6** | 🟢 | 🟢 §2.2 | 🟢 | 🟢 | 🟢 ~85% | 🟢 | 🟢 RC1 |
| **D7** | 🟢 | 🟢 §4 | 🟢 | 🟢 | 🟢 ~80% | 🟢 | 🟢 RC1/RC3 |
| **D8** | 🟢 | 🟢 §2.1,§3 | 🟢 | 🟢 | 🟡 ~75% (needs D5) | 🟢 | 🟢 O-5 |

All independence scores ≥ 60% (§6.12 floor; the 🟡 70–75% rows are *acceptable with documented
dependencies* — every such dependency appears in the dependency diagram §5). No row < 60% ⇒ §9.6
(over-connection) does **not** fire.

**Final Checks (§8.10):**

- **Traceability Sensitivity:** sampled 12 / 12 output elements successfully traced to a source location
  (each claim/instance carries a §-reference). 🟢 PASS — §9.2 (re-synthesis) does not fire.
- **Angular Diversity:** mean pairwise distance = **0.4219** (target > 0.30). 🟢 PASS.
- **Reference-Layer Coverage:** RC1 (D3, D6, D7) · RC2 (D2, D4, D8) · RC3 (D3, D7) · RC4 (D1, D2, D5) —
  **all four reference challenges covered**; no derivative is irrelevant to the reference layer (§6.15).
  🟢 PASS — §9.12 (disconnection) does not fire.

**Failure-mode sweep (§9.1–9.13):** Type-based segmentation 🟢 none · Re-synthesis 🟢 none · Isolated
components 🟢 none · Over-segmentation 🟢 (8 ≤ 10) · Under-segmentation 🟢 · Over-connection 🟢 (all ≥ 60%)
· Undocumented dependencies 🟢 (DAG complete) · False synergies 🟢 (all traced) · Missed synergies 🟢
(28-pair sweep done) · Angular clustering 🟢 (mean 0.42) · Ungrounded angular scores 🟢 (per-axis rationale
given) · Disconnection from reference 🟢 (all RCs covered) · Unconditioned synergies 🟢 (each names an RC).

---

## 11. Validation Checklist (§11)

| # | Check | Status |
|---|-------|:------:|
| 1 | Reference layer specified (well-formed per §2.2) | 🟢 |
| 2 | No type-based derivatives (no "Claims/Examples/Caveats" buckets) | 🟢 |
| 3 | Uniform composition (claims + mechanisms + instances + boundaries) | 🟢 |
| 4 | Connections intact (instances↔principles, claims↔mechanisms, boundaries↔claims) | 🟢 |
| 5 | Traceable (each output element maps to a source location) | 🟢 |
| 6 | Distinct (derivatives do not overlap > 20%) | 🟢 |
| 7 | Entanglement managed (cross-links noted at separation points) | 🟢 |
| 8 | Completeness (all source content assigned to derivative or common ground) | 🟢 |
| 9 | Dependencies documented (all functional dependencies noted, §5) | 🟢 |
| 10 | Independence sufficient (all ≥ 60%) | 🟢 |
| 11 | Angular positions evidence-based (each α,β,γ content-justified) | 🟢 |
| 12 | Angular diversity sufficient (mean pairwise = 0.42 > 0.3) | 🟢 |
| 13 | Synergies explicit | 🟢 |
| 14 | Synergies grounded (traced to source) | 🟢 |
| 15 | Synergies conditioned on reference (each names an RC / O-#) | 🟢 |
| 16 | Synergies angularly explained (complementarity stated) | 🟢 |
| 17 | Evidence package complete (claims/mechanism/instances/boundaries per derivative) | 🟢 |
| 18 | Visualization output valid (JSON per §4.5) | ⚪ deferred (not requested) |

17 / 18 🟢; one ⚪ deferred (JSON visualization export was not requested for this run).

---

## 12. Net Position in the Design Space (Narrative)

**The source is not a competing framework — it is a downward-and-outward extension that confirms the spine.**

- **Confirms (identity / near-origin).** **D5** is *literally* this repository's CQR ξ = ln(k/Λ_G) and
  discrete `S` engine — same conservation-manifold compiler, same gasification bin-level validation. **D1**
  is the memory-monotonicity invariant generalized to the full perception→memory→actuation algebra. The
  spine's core is independently restated here, which is the strongest possible attachment (β ≈ 0.05–0.10).
- **Restates the ladder.** **D3**'s reflexive / tactical / strategic stratification is near-isomorphic to
  the README's **Gate → Tracking-Buffer → Memory-Bank** RG ladder, with verification strengthening down the
  ladder exactly as the spine requires.
- **Extends downward (new substrate floor, high γ).** **D7** adds an *analog memristive device tier* beneath
  the digital-IC EPU overlays — a floor the README's hardware layer did not previously have. The τ = 5 s
  boundary keeps that floor *non-strategic*, preserving safety (RC3).
- **Extends outward (new orthogonal modules).** **D4** adds recall-side shadow-price economics the
  conserved-budget CQR lacks; **D8** adds an LLC developmental-phase-transition detector that cross-validates
  the topological invariant `S` (advancing **O-5**).
- **Anchors the projected with the measured.** **D6** supplies the **only measured latency in the corpus
  (~32 ns)**, giving the README's *projected* EPU Gate budgets an empirical reference point — the seam is
  preserved (still projected) per the Common Ground discipline.

**Effect on open obligations:** **O-4 (WCET)** reinforced (measured 32 ns consistent with topology at TB,
latched bit at Gate); **O-5 (topological mapping)** partially advanced (LLC as a second discrete phase
signal); **new O-10** device-monotonicity (PoC Workstream A; exit: violation rate → 0 under conservative
discipline on measured-device models); **new O-11** φ-transfer to automotive (same standing as O-3; the
`sct`-surrogate reproducibility study, rank corr ≥ 0.85, is owed).

**Honest flags (carried from the source's own evidentiary discipline):**
1. Every memristor retention / monotonicity / latency number is a **Phase-0 hypothesis**, not measured.
2. The *Numerical Substrate Partition* ADR (ratified decision) and the *Functionally-Driven Refinement
   Schedule* (projected plan) upgrade **no** device figure to *measured*.
3. D7's analog substrate is, by the ADR, **barred from the strategic tier** — the "new floor" is explicitly
   a *non-strategic* floor; strategic persistence stays digital Posit/quire.

---

*End of EVD Assessment — Source #1. This standalone document is the complete §8 output-format instance;*
*the README's Design-Space Integration Registry carries the condensed summary and the updated obligation/*
*validation tables. The updated README + this assessment together form the reference layer for Source #2.*
